This directory contains basic information for getting started with Doozer++.
The files contained here can be used as a basis for all build systems based
on Doozer++.  Each of the subdirectories provides an example Doozer++-baesd
build system.

Contents
--------
    common.defs.mk.in - Common makefile settings in Autoconf template form
    config.guess      - Helper script needed by configure to guess the
                        platform type
    config.sub        - Helper script needed by configure
    dppgen.sh         - Basic script for running autoconf, autoheader, and
                        aclocal
    install-sh        - Helper script needed on platforms that do not have
                        a BSD-compatible install(1) utility

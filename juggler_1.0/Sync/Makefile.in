# -----------------------------------------------------------------------------
# Makefile.in for juggler/Sync.  It requires GNU make.
# $Id$
# -----------------------------------------------------------------------------

default: all

# Include common definitions.
include @topdir@/Makefile.base

includedir	= @includedir@/Sync
srcdir		= @srcdir@
top_srcdir	= @top_srcdir@
wwwdir		= ${srcdir}/Doc
VJROOT		= ${top_srcdir}
VJ_SRCDIR	= @VJ_SRCDIR@/Sync
WORKDIR		= ${topdir}/Sync

# Include dependency and installation targets.
include ${MKPATH}/vj.dep.mk
include ${MKPATH}/vj.install.mk

#--- Search path -------
vpath
vpath %.c @srcdir@
vpath %.C @srcdir@
vpath %.o ${OBJDIR}
#------------------------

# ---- Determine what objects to use ----
ifeq (@SYNC_SYS@,POSIX)
    DEPEND_SRCS := 
else
    DEPEND_SRCS := vjMutexSGI.C vjSemaphoreSGI.C
endif

SYNC_OBJECTS	= $(addprefix $(OBJDIR)/,$(DEPEND_SRCS:.C=.o))

# -----------------------------------------------------------------------------
# Library targets.
# -----------------------------------------------------------------------------
all dbg opt dso ddso obj: ${SYNC_OBJECTS}

# -----------------------------------------------------------------------------
# Clean-up.
# -----------------------------------------------------------------------------
CLEAN_FILES +=	${SYNC_OBJECTS}

# -----------------------------------------------------------------------------
# Dependencies generated by makedepend(1).
# -----------------------------------------------------------------------------

# DO NOT DELETE THIS LINE -- make depend depends on it.


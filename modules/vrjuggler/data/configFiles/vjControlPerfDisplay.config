<ConfigChunkDB>
  <PerfData name="Kernel">
    <labels>"app preFrame()" "trigger draw()" "app intraFrame()" "drawManager sync()" "app postFrame()" "checkForReconfig()" "InputManager updateAllData()" "updateFrameData()" </labels>
  </PerfData>
  <PerfData name="vjGlPipe">
    <labels>"Wait for draw trigger" "app pipePreDraw()" 
"w1 setup"
"w1 app bufferPreDraw()"
"w1 app contextPreDraw()"
"w1vp1 setup"
"w1vp1l set projection"
"w1vp1l app draw()"
"w1vp1l draw objects/sim"
"w1vp1r set projection"
"w1vp1r app draw()"
"w1vp1r draw objects/sim"

"w2 setup"
"w2 app bufferPreDraw()"
"w2 app contextPreDraw()"
"w2vp1 setup"
"w2vp1l set projection"
"w2vp1l app draw()"
"w2vp1l draw objects/sim"
"w2vp1r set projection"
"w2vp1r app draw()"
"w2vp1r draw objects/sim"

"w3 setup"
"w3 app bufferPreDraw()"
"w3 app contextPreDraw()"
"w3vp1 setup"
"w3vp1l set projection"
"w3vp1l app draw()"
"w3vp1l draw objects/sim"
"w3vp1r set projection"
"w3vp1r app draw()"
"w3vp1r draw objects/sim"

"w4 setup"
"w4 app bufferPreDraw()"
"w4 app contextPreDraw()"
"w4vp1 setup"
"w4vp1l set projection"
"w4vp1l app draw()"
"w4vp1l draw objects/sim"
"w4vp1r set projection"
"w4vp1r app draw()"
"w4vp1r draw objects/sim"

"" "" ""
"Finish windows loop"
"Finish swapBuffers"
 </labels>
  </PerfData>
</ConfigChunkDB>

# Generated by mkmakefile.pl.

default: trackerConfig@EXEEXT@

# Basic options.
srcdir		= @srcdir@
CFLAGS		= @APP_CFLAGS@ $(EXTRA_FLAGS) $(INCLUDES) $(DEFS)
CXXFLAGS	= @APP_CXXFLAGS@ $(EXTRA_FLAGS) $(INCLUDES) $(DEFS)
DEFS		= @APP_DEFS@
EXTRA_FLAGS	= @APP_EXTRA_FLAGS@ $(DEBUG_FLAGS)
DEBUG_FLAGS	= @APP_DEBUG_FLAGS@
OPTIM_FLAGS	= @APP_OPTIM_FLAGS@
INCLUDES	= @APP_INCLUDES@ -I$(srcdir) -I$(srcdir)/common		\
                  $(GLUT_INC) $(GLUI_INC)
LINK_FLAGS	= @APP_LINK_FLAGS@ $(EXTRA_FLAGS)

# Paths to GLUT and GLUI.
GLUT_LIB	= -B static -L/home/users/allenb/Libs/glut/lib32 -lglut
GLUT_INC	= -I/home/users/allenb/Libs/glut/include

GLUI_LIB	= -B static -L/home/users/allenb/Libs/glui_v2/lib -lglui
GLUI_INC 	= -I/home/users/allenb/Libs/glui_v2

# Libraries needed for linking.
BASIC_LIBS	= @APP_BASIC_LIBS_BEGIN@ @APP_BASIC_LIBS@ @APP_BASIC_EXT_LIBS@ @APP_BASIC_LIBS_END@
EXTRA_LIBS	= @APP_EXTRA_LIBS_BEGIN@ $(GLUI_LIB) $(GLUT_LIB)	\
                  @APP_EXTRA_LIBS_GL@ -lXmu @APP_EXTRA_LIBS@ @APP_EXTRA_LIBS_END@ 

# Commands to execute.
C_COMPILE	= @APP_CC@ $(CFLAGS)
CXX_COMPILE	= @APP_CXX@ $(CXXFLAGS)
LINK		= @APP_LINK@ $(LINK_FLAGS)

VPATH	= @srcdir@:@srcdir@/common

OBJS	= sgGlutApp.@OBJEXT@ sgGlutWin.@OBJEXT@ sgInput.@OBJEXT@ sgMuiWin.@OBJEXT@ trackerConfig.@OBJEXT@

# -----------------------------------------------------------------------------
# Application build targets.
# -----------------------------------------------------------------------------
trackerConfig@EXEEXT@: $(OBJS)
	$(LINK) @EXE_NAME_FLAG@ $(OBJS) $(BASIC_LIBS) $(EXTRA_LIBS)

# Suffix rules for building object files.
.SUFFIXES: .cpp .@OBJEXT@

.cpp.@OBJEXT@:
	$(CXX_COMPILE) @OBJ_NAME_FLAG@ @OBJ_BUILD_FLAG@ $<

# -----------------------------------------------------------------------------
# Clean-up targets.
# -----------------------------------------------------------------------------
clean:
	rm -f Makedepend *.@OBJEXT@ trackerConfig.ilk  so_locations *.?db core*
	rm -rf ii_files

clobber:
	@$(MAKE) clean
	rm -f trackerConfig@EXEEXT@ 
